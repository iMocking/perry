<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.perry.app">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Issue #552: Photo Picker / ACTION_GET_CONTENT for the image picker. -->
    <!-- Photo Picker (API 33+) does not require READ_MEDIA_IMAGES, but we
         declare it for the ACTION_GET_CONTENT fallback path on Android 13+. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:label="Perry App"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!--
          Google Maps API key for the perry/ui MapView widget (#517).
          Replace `__YOUR_GOOGLE_MAPS_API_KEY__` with a real key obtained
          from Google Cloud Console (Maps SDK for Android enabled). Apps
          that don't use the MapView widget can leave the placeholder;
          the SDK only attempts to load tiles when a MapView is actually
          constructed.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="__YOUR_GOOGLE_MAPS_API_KEY__" />

        <activity
            android:name=".PerryActivity"
            android:exported="true"
            android:theme="@style/Theme.Perry.Splash"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
